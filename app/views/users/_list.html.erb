 <div class="col-md-3 ">
     <h2>User info</h2>
      
      <table class="table">
  <tbody>
  <tr>
	  <th><%= attachment_image_tag user, :profile_image, :fill, 60, 60, fallback: "no_image.jpg" %></th>
  </tr>
  <tr>
	  <th>name <%= user.name %></th>
  </tr>
  <tr>
	  <th>introduction <%= user.introduction %></th>
  </tr>
</tbody></table>
　
  <%= link_to edit_user_path(user.id), method: :get do %>
<i class="fas fa-user-cog"></i>
  <% end %>
      <h2 class="mt-3">New book</h2>
      <%= form_with model: book, local:true do |f| %>
    <label for="book_title">Title
    <%= f.text_field :title %></label>
    <label for="book_opinion">Opinion
    <%= f.text_area :body %></label>
    <%= f.submit 'Create Book' %>
     <% end %>
</div>
    